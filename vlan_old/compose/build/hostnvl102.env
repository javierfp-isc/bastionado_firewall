#Creamos rutas en el archivo /etc/routes
COMMANDS="echo ip route del default >> /etc/routes,echo ip route del 172.200.0.0/16 >> /etc/routes,echo ip route add 172.200.10.0/24 via 172.200.10.2 >> /etc/routes"

