PACKS="vim iputils-ping curl nano net-tools traceroute dnsutils procps strongswan libcharon-extra-plugins kmod chromium"
#Enrutamiento
COMMANDS="echo ip route del default > /etc/routes,echo ip route add default via 192.168.22.100 >> /etc/routes"

