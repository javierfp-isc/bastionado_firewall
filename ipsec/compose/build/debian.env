PACKS="vim iputils-ping curl nano net-tools traceroute dnsutils procps chromium strongswan libcharon-extra-plugins kmod"
#Enrutamiento
COMMANDS="echo ip route del default > /etc/routes,echo ip route add default via 192.168.22.100 >> /etc/routes"

