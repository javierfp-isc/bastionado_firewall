#Vaciamos tabla de enrutamiento, creamos rutas en el archivo /etc/routes, Levantamos a modo de prueba un website con SimpleHTTPServer
COMMANDS="DEBIAN_FRONTEND=noninteractive apt install -y iptables-persistent python,echo \"ip route del default\">>/etc/routes,echo \"ip route add 192.168.110.0/24 via 192.168.120.202\">>/etc/routes,echo \"ip route add 192.168.111.0/24 via 192.168.120.202\">>/etc/routes,echo \"ip route add 192.168.112.0/24 via 192.168.120.202\">>/etc/routes,echo \"ip route add default via 192.168.120.203\">>/etc/routes,mkdir /web,echo \"<html><body><h1>Esto es un test www</h1></body></html>\" > /web/index.html"

