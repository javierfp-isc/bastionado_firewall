#Creamos rutas en el archivo /etc/routes
COMMANDS="echo ip route del default >> /etc/routes,echo ip route add 172.100.10.0/24 via 172.100.20.100 >> /etc/routes,echo ip route add default via 172.100.20.200 >> /etc/routes"

